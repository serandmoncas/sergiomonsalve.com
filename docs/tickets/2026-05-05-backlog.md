# Backlog — sergiomonsalve.com

_Last updated: 2026-05-05_

---

## Immediate (v1 polish)

### TICKET-001 — Revert Resend `from` address after domain verification

**When:** Resend dashboard shows all 4 DNS checks green for `sergiomonsalve.com`.

**What:** In `src/lib/resend.ts` line 7, change:
```ts
from: 'Sergio Monsalve <onboarding@resend.dev>',
```
to:
```ts
from: 'Sergio Monsalve <noreply@sergiomonsalve.com>',
```
Then deploy with `npx vercel --prod --yes`.

**Why:** Current `onboarding@resend.dev` sender causes emails to land in spam.

---

### TICKET-002 — Add favicon

**What:** Create a minimal favicon (32×32 and 16×16 `.ico`, or SVG) and place it at `public/favicon.ico`. Optionally add `<link rel="icon">` to `src/app/[locale]/layout.tsx`.

**Why:** `/favicon.ico` currently returns 404 (visible in browser console).

---

### TICKET-003 — Fix GitHub → Vercel auto-deploy

**What:** Investigate why pushes to `main` on `github.com/smonsalve/sergiomonsalve.com` don't trigger Vercel builds. Check Vercel dashboard → Project Settings → Git → Connected Repository. May need to reconnect the GitHub integration.

**Current workaround:** `npx vercel --prod --yes` for manual deploys.

---

### TICKET-004 — Resend MX record for `send` subdomain

**What:** Add MX record `send.sergiomonsalve.com → feedback-smtp.us-east-1.amazonses.com` (priority 10). Namecheap standard UI does not support subdomain MX. Options:
- Switch nameservers to Cloudflare (free, full DNS control)
- Leave as-is (only affects bounce/feedback loop handling, not email delivery)

**Impact:** Low — skipping this does not break email sending.

---

## v2 Features

### TICKET-010 — Blog (MDX)

Content-driven pages at `/[locale]/blog/[slug]`. MDX files in `content/blog/`. Frontmatter: `title`, `date`, `lang`, `tags`. List page with tag filtering. No CMS in v2 — file-based.

**Stack additions:** `@next/mdx` or `next-mdx-remote`, `gray-matter`.

---

### TICKET-011 — Recipes section

Same structure as blog at `/[locale]/recipes/[slug]`. Separate content directory `content/recipes/`. Consider shared MDX pipeline with blog.

---

### TICKET-012 — Auth (admin role)

Admin login for content management and comment moderation. Use Supabase Auth (email+password or magic link). Add `role` column to `profiles` table. Protect `/admin/*` routes via middleware.

---

### TICKET-013 — Comments with moderation

Comment threads on blog posts. Schema: `comments(id, post_slug, author_name, author_email, body, approved, created_at)`. Moderation queue in admin UI. Spam protection: honeypot field + rate limiting.

---

### TICKET-014 — SEO & OG images

- Dynamic `<title>` and `<meta description>` per page/post via Next.js `generateMetadata`
- OG image generation with `@vercel/og` or static images per post
- `sitemap.xml` and `robots.txt`
- Structured data (JSON-LD) for Person schema on homepage
